{
  "kind": "collectionType",
  "collectionName": "posts",
  "info": {
    "name": "Post",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "Title": {
      "type": "string",
      "required": true
    },
    "URL": {
      "type": "uid",
      "targetField": "Title",
      "required": true
    },
    "Content": {
      "type": "richtext"
    },
    "Media": {
      "collection": "file",
      "via": "related",
      "allowedTypes": ["images", "videos"],
      "plugin": "upload",
      "required": false
    },
    "comments": {
      "collection": "comment",
      "via": "post"
    },
    "categories": {
      "collection": "category"
    }
  }
}
